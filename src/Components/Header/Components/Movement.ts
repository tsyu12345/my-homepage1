export interface Position {
    x: number;
    y: number;
}

/**
 * 
 * @param targetDOM :移動対象のDOM
 * @param st_pos :開始位置
 * @param end_pos :終了位置
 * @param speed :移動速度
 * @param repeat :繰り返すか？
 */
export const moveCharactor = (targetDOM:HTMLElement, st_pos:Position, end_pos:Position, speed:number, repeat:boolean) => {

}